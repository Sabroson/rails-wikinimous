<h1>Articles</h1>

<ul>
  <% @articles.each do |article| %>
  <li><%= link_to  article.title, article_path(article) %><br><%= article.content %></li>
  <% end %>
</ul>

<p><%= link_to 'Add new Article', new_article_path %></p>
